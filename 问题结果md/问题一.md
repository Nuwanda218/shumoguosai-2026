# 问题一 静态方案与功耗模型

## 1. 静态方案说明

静态方案假设一天 24 小时使用同一组参数：

$$
G_t = G, \quad R_t = R, \quad t = 0,1,\dots,23
$$

其中：

- G：每块 GPU 负载（%）  
- R：数据传输速率（Mbps）  

问题一中不考虑动态调度，只考虑误差率的满足。

变量范围：

$$
60 \le G \le 100, \quad 800 \le R \le 1200
$$

总处理量约束（全天）：

$$
\sum_{t=0}^{23} q_t \ge 1 \quad \Rightarrow \quad \frac{G}{80} \cdot \frac{R}{1000} \ge 1
$$

误差约束：

$$
E = 30 - 0.2 (G - 60) + 0.05 (800 - R) \le 5 \quad \Rightarrow \quad 4G + R \ge 1540
$$

---

## 2. 功耗模型说明

一天总能耗：

$$
\text{总能耗} = 24 \cdot \Big[ P_\text{gpu}(G) + P_\text{trans}(R) + P_\text{cool}(G) \Big]
$$

### 2.1 GPU 功耗

每块 GPU 功耗按负载线性计算：

$$
P_\text{gpu}(G) = 8 \cdot \text{单块 GPU 功率} / 1000
$$

说明：计算 8 块 GPU 的总功耗（单位 kW），随负载增加线性增长。

### 2.2 数据传输功耗

传输速率功耗线性增加：

$$
P_\text{trans}(R) = 0.25 + 0.0004 \cdot (R - 800)
$$

说明：基础功耗 0.25 kW，每增加 1 Mbps 数据传输速率，功耗增加 0.0004 kW。

### 2.3 冷却功耗

冷却功耗随 GPU 负载线性增加：

$$
P_\text{cool}(G) = 1.2 + 0.12 \cdot (G - 60)
$$

说明：基础功耗 1.2 kW，每增加 1% GPU 负载，冷却功耗增加 0.12 kW。

---

## 3. 数据分析误差约束

采用 8 块 GPU 误差累加模型：

$$
E = 30 - 0.025 \cdot 8 \cdot (G - 60) + 0.05 \cdot (800 - R)
$$

化简为：

$$
E = 30 - 0.2 (G - 60) + 0.05 (800 - R)
$$

约束条件：

$$
E \le 5 \quad \Rightarrow \quad 4G + R \ge 1540
$$

---

## 4. 总能耗目标函数

一天总能耗：

$$
\text{总能耗} = 24 \cdot \Big[ P_\text{gpu}(G) + P_\text{trans}(R) + P_\text{cool}(G) \Big]
$$

功耗分解：

$$
\begin{aligned}
P_\text{gpu}(G) &= 8 \cdot \text{单块 GPU 功率} / 1000 \\
P_\text{trans}(R) &= 0.25 + 0.0004 \cdot (R - 800) \\
P_\text{cool}(G) &= 1.2 + 0.12 \cdot (G - 60)
\end{aligned}
$$

---

## 5. 最优解

$$
G = 85\%, \quad R = 1200 \text{ Mbps}
$$

| 指标         |       数值 |
| ------------ | ---------: |
| GPU 负载     |        85% |
| 数据传输速率 |  1200 Mbps |
| GPU 集群功率 |    2.00 kW |
| 数据传输功率 |    0.41 kW |
| 冷却功率     |    4.20 kW |
| 系统总功率   |    6.61 kW |
| 一天总能耗   | 158.64 kWh |
| 总处理量     |      1.275 |
| 数据分析误差 |         5% |

## 最优解分析与解释

最优解 G=85\%, R=1200 Mbps 的核心原因是：

- 提高传输速率是“低功耗、高降误差”的手段  
- 提高 GPU 负载是“高功耗、也能降误差”的手段  

误差模型为：

$$
E = 30 - 0.2(G - 60) + 0.05(800 - R)
$$

等价约束：

$$
E \le 5 \quad \Longleftrightarrow \quad 4G + R \ge 1540
$$

当 R=1200 时：

$$
4G + 1200 \ge 1540 \quad \Rightarrow \quad G \ge 85
$$

- 如果 GPU 低于 85%，误差超过限制  
- 如果 GPU 高于 85%，误差更低，但功耗增加，没有必要  

传输速率取最大值的原因：

| 传输速率 R | 传输功率 P_\text{trans} |
| ---------- | ----------------------- |
| 800 Mbps   | 0.25 kW                 |
| 1200 Mbps  | 0.41 kW                 |

- 仅增加 0.16 kW，却能将误差率下降约 20 个百分点  
- 相比之下，提高 GPU 负载会显著增加 GPU 功耗和冷却功耗  

任务量约束在最优点处不是最紧约束：

$$
\text{日任务完成量} = \frac{85}{80} \cdot \frac{1200}{1000} = 1.275 \ge 1
$$

因此，真正决定最优点的是误差约束，而不是任务量约束。

### 综合解释

1. 为满足 5\% 误差约束，必须提高计算能力或传输速率  
2. 提高传输速率功耗代价小，降误差效果显著，因此优先将 R 提高到上限 1200 Mbps  
3. 在 R$已达到上限后，只需将 GPU 负载提高到 85%，误差刚好达到 5%  
4. 继续提高 GPU 会增加功耗，但不会带来额外收益  
5. 因此最小能耗解为 G=85\%, R=1200 Mbps  

> 注：该结果完全依赖题目给定参数，尤其是“传输速率提升对误差改善大、对功耗影响小”的设定。最优解贴近传输速率上限，是模型参数共同作用的数学结果，并非程序错误。

## 图1：误差率三维高度图

![image-20260519113140082](C:\Users\Nuwanda\AppData\Roaming\Typora\typora-user-images\image-20260519113140082.png)

**坐标轴：**

- X轴：GPU负载 G（%）  
- Y轴：数据传输速率 R（Mbps）  
- Z轴：误差率 E（%）  

**绘制内容：**

1. 误差率三维曲面  
2. 半透明的 E = 5\% 约束平面  
3. 最低误差角点 (G=100, R=1200, E=2)$  
4. 最优点 (G=85, R=1200, E=5)$ 
5. 标注“最优点”和“最低误差2%”  

**预期解释：**

- GPU负载越高，误差率越低  
- 传输速率越高，误差率越低  
- 最优点位于 E=5\% 的约束边界  
- 最低误差点说明理论最低误差在 GPU 和传输速率同时取上限位置  

---

## 图 2：总功耗三维高度图

![image-20260519113441117](C:\Users\Nuwanda\AppData\Roaming\Typora\typora-user-images\image-20260519113441117.png)

**坐标轴：**

- X轴：GPU负载 G（%）  
- Y轴：数据传输速率 R（Mbps）  
- Z轴：系统总功率 P（kW）  

**系统总功率公式：**
$$
P(G,R) = P_\text{gpu}(G) + P_\text{trans}(R) + P_\text{cool}(G)
$$

**绘制内容：**

1. 总功率三维曲面  
2. 最优点 (G=85, R=1200, P=6.61)
3. 标注“最优点”  

**预期解释：**

- GPU负载增加同时提高 GPU 功耗和冷却功耗，总功率沿 G 上升更快  
- 传输速率方向功率几乎不上升
- 模型倾向提高传输速率来降低误差，而不是增加 GPU 负载  

---

##  图 3：二维可行域与总功率等高线图

![image-20260519113425577](C:\Users\Nuwanda\AppData\Roaming\Typora\typora-user-images\image-20260519113425577.png)

**坐标轴：**

- X轴：GPU负载 G（%）  
- Y轴：数据传输速率 R（Mbps）  

**绘制内容：**

1. 任务量边界：

$$
\frac{G}{80} \cdot \frac{R}{1000} = 1
$$

2. 误差边界：

$$
4G + R = 1540
$$

3. 不可行区域浅灰色遮罩  
4. 可行区域用总功率色块填充  
5. 最优点 (85, 1200)$ 
6. 图例左下角，功率颜色说明右侧中部  

**预期解释：**

- 最优点位于传输速率上限与误差边界交汇  
- 任务量约束不是最紧约束，误差约束决定最优点位置  
- 可行域内颜色展示总功率相对高低，低功率边界显示最优点  

---

